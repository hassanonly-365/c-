#include<iostream>
#include<cmath>  // library <cmath> is required for maths functions
using namespace std;

int main()
{
    double x =3.99999;
    double y = 4;
    double z;
     
    // z = max(x,y); // max function is used to find the grater value.
    // z = min(x,y); // min function is used to find the smallest value. 
    // z = pow(x,y); // pow function is used to find the power of a number.
    // z = sqrt(y);  // sqrt function is used to find the square root of a function.
    // z =  abs(-3); // abs() returns the absolute value of an integer number.
    // z = round(x); // round() functions rounds the decimal number to nearest whole no.
    // z= ceil(x);   // ceil() rounds the decimal no to the next whole no.
    // z = floor(x); // floor()rounds the decimal no to the previous whole no.

    cout <<z;

    return 0;
}

